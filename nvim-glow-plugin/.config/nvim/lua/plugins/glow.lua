-- Glow plugin for markdown preview in-buffer
-- Requires glow CLI to be installed (via install-glow.sh)
-- Usage: :Glow to preview current markdown buffer

return {
  "ellisonleao/glow.nvim",
  config = true,
  cmd = "Glow",
}
